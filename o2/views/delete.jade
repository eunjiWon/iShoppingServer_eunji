doctype html
html
    head
        meta(charset='utf-8')
    body
        h1 
            a(href='/topic') 가스리 친구들 안녕
        ul  
            each topic in topics
                li
                    - rid = topic._id
                    a(href='/topic/'+rid)= topic.name
        artcle
          h1= 'Delete? ' + _topic.name
          - rid = _topic._id
          form(action='/topic/' + rid + '/delete' method='post')
             p
                  input(type='submit' value='YES')
          a(href='/topic/' + rid) No
                 
